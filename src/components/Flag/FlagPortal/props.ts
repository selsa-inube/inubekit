const parameters = {
  docs: {
    description: {
      component:
        "FlagPortal is a component that renders flags in a portal, displaying them in a fixed position on the screen. It works in conjunction with FlagProvider and useFlag hook to manage flag notifications.",
    },
  },
};

const props = {};

export { parameters, props };
