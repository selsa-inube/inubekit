const parameters = {
  docs: {
    description: {
      component:
        "A versatile table head component, which allows customization of row borders, border styles, and zebra striping.",
    },
  },
};

const props = {
  children: {
    control: { disable: true },
    description: "Content to be rendered inside the **Thead** component.",
  },
};

export { props, parameters };
